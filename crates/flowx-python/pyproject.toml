[project]
name = "flowx"
version = "0.1.0"
description = "FlowX Python bindings"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
module-name = "flowx"
bindings = "pyo3"
